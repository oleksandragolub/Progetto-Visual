Descrizione delle cartelle e dei file:

***

- cartella "versioni_scartate"

Contiene versioni precedenti del codice con configurazioni di parametri che sono state testate ma successivamente scartate. Si tratta di esperimenti preliminari non inclusi nei risultati finali.

***

- cartella "senza seed"

Contiene versioni dei modelli con le stesse configurazioni presenti nella cartella "con seed", ma senza fissare la random seed. Di conseguenza:
- i risultati non sono perfettamente riproducibili;
- le metriche possono variare tra esecuzioni diverse.
Questi file rappresentano esperimenti preliminari prima dell’introduzione della riproducibilità.

***

- cartella "con seed"

Contiene i modelli finali utilizzati per il confronto nel Task 1. In questa cartella:
- la random seed è fissata per garantire riproducibilità;
- le configurazioni di preprocessing e data augmentation sono mantenute costanti;
- varia esclusivamente l’architettura del modello (ResNet, EfficientNet, due varianti SE-Resnet).
Questa è la cartella di riferimento per i risultati ufficiali, visti nella presentazione.


***

- file "metriche-task1.pdf"

File riepilogativo contenente le metriche calcolate per ciascun modello presente nella cartella "con seed".
Per ogni modello vengono riportate:
- Accuracy
- Precision (macro)
- Recall (macro)
- Macro F1-score
Il documento fornisce una sintesi comparativa delle performance.
